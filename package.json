{
  "name": "grafana-docker-armv6",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "check-latest": "node dist/index.js --print-json",
    "download": "node dist/index.js --download",
    "test": "npm run build && npx playwright install --with-deps chromium && node --test dist/test/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0-only",
  "type": "module",
  "dependencies": {
    "@types/node": "^26.2.0",
    "playwright": "^1.62.1",
    "typescript": "^7.0.2"
  }
}
